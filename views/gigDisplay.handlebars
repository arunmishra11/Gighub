{{!-- 
<div class="border-4">
  <div class="border-4 flex justify-center">
    <h1>{{title}}</h1>
  </div>
</div> --}}

{{!-- <div class="card w-96 bg-base-100 shadow-xl border-2 mx-auto">
  <div class="card-body ">
    <h2 class="card-title mb-3 mx-auto">{{ title }}</h2>
    <p class="indent-5">{{ description }}</p>
      <button class="btn btn-primary mt-8">Contact Now <p><strong>Email:</strong> {{contact_email}}</p></button>
    </div>
  </div>
</div> --}}

<div class="card w-96 bg-base-100 shadow-xl border-2 mx-auto">
  <div class="card-body">
    <h2 class="card-title mb-3 mx-auto">{{ title }}</h2>
    <p class="indent-5">{{ description }}</p>
    <p><strong>Email:</strong> <a href="mailto:{{contact_email}}">{{contact_email}}</a></p>
    <a href="mailto:{{contact_email}}" class="btn btn-primary mt-4">Contact Now</a>
  </div>
</div>